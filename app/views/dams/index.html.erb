<h3>Dams</h3>

<% if @current_user.present? && @current_user.admin == true %>
    <%= link_to 'Add Dam', new_dam_path %>
<% end %>

<ul>
<% @dams.each do |dam| %>
    <li>
        <%= link_to dam.name, dam_path(dam.id) %>
    </li>
    <% end %>
</ul>

